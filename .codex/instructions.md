# Codex Instructions

Use the repository root as working directory. Prefer targeted edits. After changes, run tests and build when dependencies are installed.

If dependencies are unavailable, still update code and report which commands could not be executed.
